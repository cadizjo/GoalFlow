version: "3.9"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: goalflow
      POSTGRES_PASSWORD: goalflow
      POSTGRES_DB: goalflow
    ports:
      - "5432:5432"
